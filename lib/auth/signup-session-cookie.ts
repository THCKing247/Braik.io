/** HttpOnly cookie: allows access to /signup/* when waitlist mode is on (e.g. parent flow after code validation). */
export const BRAIK_SIGNUP_SESSION_COOKIE = "braik_signup_session"

export const BRAIK_SIGNUP_SESSION_MAX_AGE_SEC = 60 * 60 * 4
