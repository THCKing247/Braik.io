import { DashboardRouteTransitionPulse } from "@/components/portal/dashboard-route-skeletons"

/** TODO(cleanup): Match study-guide card skeletons here once those surfaces stabilize. */
export default function Loading() {
  return <DashboardRouteTransitionPulse aria-label="Loading study guides" />
}
