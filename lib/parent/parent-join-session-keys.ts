/** Session keys for the public parent onboarding flow (`/parent/join`). */

export const BRAIK_PARENT_PLAYER_CODE_SESSION_KEY = "braik_parent_player_code"

/** JSON: `{ playerDisplayName?: string | null; teamName?: string | null }` — set after code validation. */
export const BRAIK_PARENT_JOIN_PREVIEW_SESSION_KEY = "braik_parent_join_preview"
